DelSol Integration (Prepared)

This folder contains the integration surface for DelSol (facturación/contable/laboral).

Currently the client is a stub: it does **not** call external services.
When you decide to integrate, only the client implementation should change.
